<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.skyeye.eve.dao.ReportTypeDao">

	<select id="queryReportTypeList" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			a.id,
			a.type_name typeName,
			a.state state,
			a.order_by orderBy,
			b.user_name createId,
			CONVERT(a.create_time, char) createTime
		FROM
			forum_report_type a
			LEFT JOIN sys_eve_user_staff b ON a.create_id = b.user_id
		WHERE a.state != 4
			<if test="typeName != '' and typeName != null">
			     AND a.type_name LIKE '%${typeName}%'
		    </if>
			<if test="state != '' and state != null">
				AND a.state = #{state}
			</if>
			ORDER BY a.order_by ASC
	</select>
	
	<select id="queryReportTypeMationByTypeName" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			a.id
		FROM
			forum_report_type a	
		WHERE 
			a.type_name = #{typeName}
			AND a.state != 4
	</select>
	
	<select id="queryReportTypeMationByTypeNameAndId" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			a.id
		FROM
			forum_report_type a	
		WHERE 
			a.type_name = #{typeName}
			AND a.id != #{id}
			AND a.state != 4
	</select>
	
	<select id="queryReportTypeAfterOrderBum" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			a.order_by orderBy
		FROM
			forum_report_type a
		WHERE a.state != 4
		ORDER BY a.order_by DESC 
		LIMIT 1
	</select>
	
	<insert id="insertReportTypeMation" parameterType="java.util.Map">
	    INSERT INTO forum_report_type 
	    (id, type_name, state, order_by, create_id, create_time)
	    VALUES(#{id}, #{typeName}, #{state}, #{orderBy}, #{createId}, #{createTime})
	</insert>
	
	<select id="queryReportTypeMationToEditById" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			a.id,
			a.state,
			a.type_name typeName
		FROM
			forum_report_type a
		WHERE a.id = #{id}
	</select>
	
	<select id="queryReportTypeMationStateById" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			a.state
		FROM
			forum_report_type a
		WHERE a.id = #{id}
	</select>
	
	<update id="editReportTypeMationById" parameterType="java.util.Map">
		UPDATE forum_report_type
		<set>
			<if test="typeName != '' and typeName != null">
				type_name = #{typeName},
			</if>
		</set>
		WHERE id = #{id}
	</update>
	
	<select id="queryReportTypeISTopByThisId" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			b.id,
			b.order_by orderBy,
			a.order_by thisOrderBy
		FROM
			forum_report_type a,
			forum_report_type b
		WHERE
			a.id = #{id}
		AND a.order_by > b.order_by
		AND b.state != 4
		ORDER BY b.order_by DESC LIMIT 1
	</select>
	
	<update id="editReportTypeSortTopById" parameterType="java.util.Map">
		UPDATE forum_report_type
		<set>
			order_by = #{orderBy},
		</set>
		WHERE id = #{id}
	</update>
	
	<select id="queryReportTypeISLowerByThisId" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			b.id,
			b.order_by orderBy,
			a.order_by thisOrderBy
		FROM
			forum_report_type a,
			forum_report_type b
		WHERE
			a.id = #{id}
		AND b.order_by > a.order_by
		AND b.state != 4
		ORDER BY b.order_by ASC LIMIT 1
	</select>
	
	<update id="editReportTypeSortLowerById" parameterType="java.util.Map">
		UPDATE forum_report_type
		<set>
			order_by = #{orderBy}
		</set>
		WHERE id = #{id}
	</update>
	
	<update id="deleteReportTypeById" parameterType="java.util.Map">
		UPDATE forum_report_type
		<set>
			state = 4
		</set>
		WHERE id = #{id}
	</update>
	
	<update id="editReportTypeUpTypeById" parameterType="java.util.Map">
		UPDATE forum_report_type
		<set>
			state = 2
		</set>
		WHERE id = #{id}
	</update>
	
	<update id="editReportTypeDownTypeById" parameterType="java.util.Map">
		UPDATE forum_report_type
		<set>
			state = 3
		</set>
		WHERE id = #{id}
	</update>
	
	<select id="queryReportTypeUpList" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT
			a.id,
			a.type_name name
		FROM
			forum_report_type a
		WHERE a.state =2
			ORDER BY a.order_by ASC
	</select>
	
</mapper>