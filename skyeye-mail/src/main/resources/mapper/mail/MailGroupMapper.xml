<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.skyeye.eve.dao.MailGroupDao">

    <select id="queryMailMationTypeList" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
            a.id,
            a.`name`,
            a.`desc`,
            COUNT(b.id) perpleNum
        FROM
            sys_mail_type a
                LEFT JOIN sys_mail_list b ON a.id = b.type_id
        WHERE
            a.create_id = #{userId}
        GROUP BY a.id
    </select>

    <select id="queryMailMationTypeByName" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
            a.id
        FROM
            sys_mail_type a
        WHERE
            a.create_id = #{userId}
          AND a.`name` = #{name}
    </select>

    <insert id="insertMailMationType" parameterType="java.util.Map">
        INSERT into sys_mail_type
            (id, `name`, `desc`, create_id, create_time)
        VALUES
            (#{id}, #{name}, #{desc}, #{createId}, #{createTime})
    </insert>

    <select id="queryMailMationTypeByIdAndUserId" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
            *
        FROM
            (SELECT
                 a.id,
                 a.`name`,
                 a.`desc`,
                 COUNT(b.id) perpleNum
             FROM
                 sys_mail_type a
                     LEFT JOIN sys_mail_list b ON a.id = b.type_id
             WHERE
                 a.create_id = #{userId}
               AND a.id = #{id}
             GROUP BY a.id
            ) a
        WHERE
            a.perpleNum = 0
    </select>

    <delete id="deleteMailMationTypeById" parameterType="java.util.Map">
        DELETE
        FROM
            sys_mail_type
        WHERE
            id = #{id}
          AND create_id = #{userId}
    </delete>

    <select id="queryMailMationTypeToEditById" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
            a.id,
            a.`name`,
            a.`desc`
        FROM
            sys_mail_type a
        WHERE
            a.id = #{id}
    </select>

    <update id="editMailMationTypeById" parameterType="java.util.Map">
        UPDATE sys_mail_type
        <set>
            <if test="name != '' and name != null">
                `name` = #{name},
            </if>
            `desc` = #{desc},
        </set>
        WHERE id = #{id}
        AND create_id = #{userId}
    </update>

    <select id="queryMailMationTypeListToSelect" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
            a.id,
            a.`name`
        FROM
            sys_mail_type a
        WHERE
            a.create_id = #{userId}
    </select>

</mapper>